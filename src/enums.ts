export enum GitSource {
    ALL = 'all',
    GITHUB = 'github',
    GITLAB = 'gitlab',
}

export enum ApiPaths {
    BASE_API_URI = 'BASE_API_URI',
    USER = 'USER',
    INFO = 'INFO',
    REPOS = 'REPOS',
}